program AppCadPedidos;

uses
  System.StartUpCopy,
  FMX.Forms,
  Base.Form.View in '..\..\view\FormBase\Base.Form.View.pas' {Form2},
  Login.Form.View in '..\..\view\Login.Form.View.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
