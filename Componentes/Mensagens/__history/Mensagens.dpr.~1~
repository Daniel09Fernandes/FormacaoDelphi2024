program Mensagens;

uses
  Vcl.Forms,
  Form.Principal.Mensagens in 'Form.Principal.Mensagens.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.MainFormOnTaskbar := True;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
